#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include "lifefunc.h"

const int MAX = 40;

int main(int argc, char *argv[]) {
	char current[MAX][MAX];
	char temp[MAX][MAX];
	printf("%d\n", argc);
	return 0;
}
